import Modal from "../components/modal"
import { DialogOverlay, DialogContent } from "@reach/dialog"
import { motion } from "framer-motion"

function useModal(styles, onDismiss) {
  return (
    
  )
}

export default useModal
